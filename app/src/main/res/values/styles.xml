<resources>

    <!-- Base application theme. -->
    <style name="BaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->

        <item name="colorAccent">@color/color_pink</item>
        <item name="android:windowBackground">@color/transparent</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:fitsSystemWindows">false</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Translucent</item>

        <item name="android:colorPrimary">@color/color_text_red</item>

        <item name="android:colorControlNormal">@color/color_line_red</item>
        <item name="android:colorControlActivated">@color/color_line_red</item>
    </style>




</resources>
